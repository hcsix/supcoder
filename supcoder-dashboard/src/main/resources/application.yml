spring:
  profiles:
    active: db, core, dashboard
  messages:
    encoding: UTF-8

server:
  port: 8083

logging:
  level:
    root:  ERROR
    org.springframework:  ERROR
    org.mybatis:  ERROR
    com.supcoder.hub.dashboard:  DEBUG
    com.supcoder.hub:  ERROR

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
